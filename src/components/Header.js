import React from 'react';
import '../style/Header.scss'

function Header() {
    return(
        <header className="main-header">
            <h1>QuizApp!</h1>
        </header>
    )
}

export default Header